extends ../layout
include ../mixins/video

block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.username
    div.home__content
        each video in user.videos
            +video(video)
        else
            span.home__empty__message No videos found